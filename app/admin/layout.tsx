export const metadata = {
  title: 'Admin — SimplifyQA',
  robots: { index: false, follow: false },
};

export default function AdminParentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
